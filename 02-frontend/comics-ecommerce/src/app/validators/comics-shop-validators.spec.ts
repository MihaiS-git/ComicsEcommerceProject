import { ComicsShopValidators } from './comics-shop-validators';

describe('ComicsShopValidators', () => {
  it('should create an instance', () => {
    expect(new ComicsShopValidators()).toBeTruthy();
  });
});
